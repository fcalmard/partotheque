{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Logins</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Datecreateat</th>
                <td>{{ entity.datecreateAt|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('logins') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('logins_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
